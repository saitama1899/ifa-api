function generatePrompt(curriculumInfo, offerInfo) {
  // Reemplaza esta línea con tu lógica para generar el prompt
  const prompt = `Aquí va tu lógica para generar el prompt basado en los datos: ${JSON.stringify(curriculumInfo)}`;
  return prompt;
}

module.exports = generatePrompt;